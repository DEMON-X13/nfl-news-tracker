/* The weeks the site knows about, in order. The page shows the LAST one.
   To publish a new week: add data/weekN.js (const WEEKN = {...}), add its <script> tag
   to index.html above data/weeks.js, then append WEEKN here. */
const WEEKS = [WEEK1];
